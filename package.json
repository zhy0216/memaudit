{
  "name": "memaudit",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "memaudit": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts src/adapters/memorybench.ts --format esm --dts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "fetch:memorybench": "npm run build && node scripts/fetch-memorybench.js"
  },
  "dependencies": {
    "@ai-sdk/openai": "^0.0.40",
    "ai": "^3.0.0",
    "apache-arrow": "^21.1.0",
    "commander": "^12.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
